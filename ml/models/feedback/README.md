# Offline feedback candidates

Local `train-feedback` runs may write a **CANDIDATE** model here.

Joblibs are gitignored. They must never replace `ml/models/xgb_fraud.joblib` (`xgb-iforest-v1-calibrated`). There is no activation endpoint.
